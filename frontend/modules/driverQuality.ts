import { DashboardModule } from './types';

export const driverQualityModule: DashboardModule = {
  id: 'driver-quality',
  title: 'Driver Quality Rankings',
  description: 'Six-week trailing safety & quality metrics from the DSP Overview Dashboard CSV. Drivers ranked Platinum → Gold → Silver → Bronze by overall score. Used for roster priority ordering.',
  href: '/driver-quality',
};
