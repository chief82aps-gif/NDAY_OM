import { DashboardModule } from './types';

export const driversModule: DashboardModule = {
  id: 'drivers',
  title: 'Driver Profiles',
  description: 'Full driver roster — source (ADP/schedule), Slack-linking status, last seen on schedule, and flagged-inactive drivers.',
  href: '/drivers',
};
