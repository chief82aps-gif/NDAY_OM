import { DashboardModule } from './types';

export const opsIngestModule: DashboardModule = {
  id: 'ops-ingest',
  title: 'Ops Channel Ingest',
  description: 'Drop any report file in #nday-operations-management with a description — it appears in the queue automatically. Click Ingest to load it.',
  href: '/ops-ingest',
};
